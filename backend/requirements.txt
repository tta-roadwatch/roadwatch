fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
psycopg[binary,pool]>=3.2,<4.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
python-dotenv>=1.0,<2.0
ijson>=3.3,<4.0
# AI 점검 리포트 실호출. 키가 없으면 템플릿으로 폴백하므로 선택 의존이지만,
# 심사자가 키를 넣으면 바로 동작하도록 이미지에 포함한다.
anthropic>=0.40,<1.0
# 로그인 토큰. 비밀번호 해싱은 표준 라이브러리(PBKDF2)를 쓰므로 추가 의존이 없다.
PyJWT>=2.8,<3.0
pyproj>=3.7,<4.0

# 인수 기준 회귀 테스트 (make test). 런타임에는 쓰이지 않지만 심사자가
# 컨테이너 안에서 바로 검증할 수 있도록 이미지에 포함한다.
pytest>=8.0,<9.0
