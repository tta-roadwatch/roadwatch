# 이벤트 임계값 결정 기록

`pipeline/explore.py`를 원본 전체(990,684건)에 돌린 분포 분석 결과와, 그에 근거한 결정.
계획 §3.4의 미결정 5건 + Phase 2에서 이월된 지표 수 문제를 여기서 확정한다.

실행일: 2026-08-28 · 근거 데이터: `data/raw/` 16개 파일

---

## A. `autonm_flg` — 자율주행 해제 판정

**실측 분포** (BSM 3세션, 값별 건수·중앙속도·주행중 비율):

| 세션 | 값 1 | 값 2 |
|---|---|---|
| 2022-10-03 | 86.3% · p50=0.00 m/s · 주행중 12% | 13.7% · p50=7.19 · 주행중 57% |
| 2023-02-23 | 75.8% · p50=0.00 · 주행중 18% | 24.2% · p50=6.22 · 주행중 68% |
| 2024-01-02 | 81.0% · p50=0.00 · 주행중 34% | 18.9% · p50=4.26 · 주행중 57% |

**결정: `2` = 자율주행 해제(수동 운행). 전 세션 공통 — 코드북(반전) 불필요.**

근거:
- 세 세션의 패턴이 동일하다. 앞서 3건 표본에서 의심했던 세션별 반전은 **전체 분포에서는 나타나지 않는다** (표본이 작아서 생긴 착시였다)
- 값 2는 소수(14~24%)이고 주행 중 비율이 높다 — "특정 구간을 수동으로 통과한다"는 운영 서사와 일치
- 값 1이 해제라면 자율주행 실증단지에서 76~86%가 수동 운행이라는 뜻이 되어 전제와 모순

## B. `auto_sttus` / `ster_sttus` / `ve_sttus` — 주행상태 이탈

**실측: 세 필드는 완전 상관이다.** 3세션 모두 조합이 `(1,1,1)` 아니면 `(0,0,0)` 뿐이다.

| 세션 | (1,1,1) | (0,0,0) |
|---|---|---|
| 2022-05-16 | 29,867 (66%) | 15,133 (34%) |
| 2022-07-25 | 71,063 (94%) | 4,647 (6%) |
| 2022-08-05 | 23,859 (91%) | 2,378 (9%) |

**결정 1: 세 필드는 하나의 신호다 → `state_deviation` 단일 이벤트로 통합.**
**결정 2: `0` = 이탈(1차 가설).** 다수값 1을 정상으로 본다. Phase 6 캘리브레이션에서
목표 셀(53/64초)이 재현되지 않으면 반대 해석을 검증한다.

**이월 문제 해소 — 07-25 세션 지표 수:** 세 필드가 한 신호이므로 07-25의 실질 지표는
`speed_shortfall` + `state_deviation` = **2종**. 기획서의 "2종"과 일치한다.
기획서의 "4종"(목표속도미달·자동화상태이탈·경로장애물밀집·주행상태코드이탈)은
완전 상관인 두 필드를 별개로 센 것이다 — **실질 신호 기준으로는 3종**이며,
파이프라인은 실질 신호를 쓴다. 기획서·화면 표기는 Phase 8에서 일괄 갱신한다.

## C. `ve` / `goal_ve` — 목표속도 미달

**실측:** p50=1.00 (차량이 목표속도를 정확히 추종). 좌측 꼬리만 존재.
목표 ≤0.5 m/s(정지·서행 지시)가 29~36%라 미달 개념이 성립하지 않아 제외.

| 임계 | 05-16 | 07-25 | 08-05 |
|---|---|---|---|
| ratio < 0.3 | 0.5% | 0.4% | 0.4% |
| **ratio < 0.5** | **1.5%** | **0.8%** | **0.5%** |
| ratio < 0.7 | 4.0% | 1.8% | 1.2% |

**결정: `goal_ve ≥ 1.0 m/s` 이고 `ve/goal_ve < 0.5` → `speed_shortfall`.**
드문 이벤트(0.5~1.5%)로 정의된다 — "지시 속도의 절반도 못 내는 상태".

## D. `obcl_nmbr` — 경로 장애물 밀집

**실측** (조인 대상 2세션, 초당 최대값 기준):

| | 05-16 | 08-05 |
|---|---|---|
| 초당 최대 p90 | 5.0 | 5.0 |
| **≥ 5 비율** | **11.6%** | **12.6%** |
| ≥ 10 | 0% | 0% |

**결정: 그 초의 `obcl_nmbr` 최대 ≥ 5 → `obstacle_density`.**
두 세션에서 같은 꼬리(≈12%)를 만드는 자연 경계이고, 10 이상은 존재하지 않는다.

## E. GPS 초당 레코드 — 초 대표 위치

**실측:** 정확히 초당 ~100건(100Hz). 같은 초 안의 좌표 이동은 p99 ≤ 7.4m — 셀 크기 40m의 1/5.

**결정: 초의 첫 레코드 좌표를 대표 위치로 쓴다.** 어떤 레코드를 골라도 셀 배정이
달라질 여지가 거의 없으므로 가장 단순한 규칙을 택한다.

관측초(GPS 기준): 05-16 = 901초 · 07-25 = 1,518초 · 08-05 = 526초.
OBJECT는 GPS보다 넓은 구간을 커버하므로(05-16에서 1,592초) 위치 없는 초는 조인에서 탈락한다 — 기획서의 GPS 기준축 설계와 일치.

---

## 확정된 이벤트 정의 (events.py에 구현)

| 이벤트 | 갈래 | 정의 (초 단위) |
|---|---|---|
| `emergency` | A (BSM) | 코드북 적용 후 비상정지·센서장애 플래그 중 하나라도 true |
| `autonomy_disengage` | A (BSM) | `autonm_flg == 2` |
| `state_deviation` | B | `auto_sttus == 0` (세 필드 완전 상관 — 하나만 본다) |
| `speed_shortfall` | B | `goal_ve ≥ 1.0` 이고 `ve/goal_ve < 0.5` |
| `obstacle_density` | B | 초 내 `obcl_nmbr` 최대 ≥ 5 |

세션별 실질 지표: BSM 3세션 = 2종 · 05-16/08-05 = 3종 · 07-25 = 2종.

## 캘리브레이션 유보 항목

Phase 6에서 목표 셀(53/64 · 113/133 · 47/60 · 34/50)이 재현되지 않을 때 순서대로 재검토:
1. B의 이탈 값 해석 (`0`↔`1`)
2. C의 임계비 (0.5 → 0.3 또는 0.7)
3. D의 임계 (5 → 4 또는 6)

---

## F. Phase 6 캘리브레이션 — 격자 규약과 이벤트 정의 확정

### F-1. 격자 규약 (Phase 5, 분모)

6개 후보를 스윕한 결과 **반올림 상수 + 판교 앵커**가 유일하게 두 목표 셀을 모두 잡았다.

```
DLAT = 0.00036      DLON = 0.00045
LAT0 = 37.3957615   LON0 = 127.1027615   (판교 분석 영역 좌하단)
```

| 셀 | 중심 좌표 | 기획서 좌표 | 05-16 | 08-05 |
|---|---|---|---|---|
| `21:4` | 37.40350, 127.10479 | 37.40342, 127.10473 | **64** ✓ | **133** ✓ |
| `1:13` | 37.39630, 127.10884 | 37.39622, 127.10878 | **60** ✓ | 49 (목표 50) |

셀 중심이 기획서 좌표와 10m 이내다. 관측초 4개 중 3개가 정확히 일치.

**초 대표 위치 규칙 변경**: Phase 3에서 "첫 레코드"로 정했으나, 100Hz로 최대 7m를
이동하므로 한 초가 셀 경계에 걸치면 **양쪽 셀 모두에서 관측**된 것으로 세야 한다.
이 규칙이 오차를 4 → 1로 줄였다. `grid.py`가 이 규칙을 쓴다.

### F-2. 이벤트 정의 (Phase 6, 분자)

`auto_sttus` 기반 정의는 **불가능**하다. 목표 셀에서 값이 전부 `1`이라
`0=이탈`이면 11%, `1=이탈`이면 100%로 양극단이 나온다 (목표는 83%).

목표 셀의 원신호를 프로파일링한 결과 **차량 속도**가 답이었다.
`08-05` 두 셀에서 "그 초의 최저 속도 < 0.5 m/s"인 초 수가 목표 이벤트초와
**정확히 일치**(113, 34)했다. 임계·집계를 스윕해 최적을 찾았다.

```
low_speed :  min(ve) < 2.0 m/s   — 그 초에 한 번이라도 7.2 km/h 아래로 떨어짐
```

| 조합 | 05-16 21:4 | 05-16 1:13 | 08-05 21:4 | 08-05 1:13 | 오차 |
|---|---|---|---|---|---|
| **저속만** | 52 (53) | **47** ✓ | 116 (113) | 37 (34) | **7** |
| 저속 + 상태이탈 | 52 | 47 ✓ | 116 | 37 | 7 |
| 저속 + 장애물 | 58 | 53 | 116 | 37 | 17 |

**채택: 저속만.** `state_deviated`는 목표 셀에서 아무것도 더하지 않고(52→52),
`obstacle_density`를 OR로 더하면 오차가 두 배 이상 나빠진다.
장애물·상태 신호는 이벤트 판정에서 빼고 `event_types` 참고 지표로만 남긴다.

### F-3. 잔여 오차와 판단

총 306관측 중 **오차 7 (2.3%)** — 계획 §5의 폴백 임계 ±5% 이내다.

| | 실측 | 기획서 |
|---|---|---|
| 05-16 `21:4` | 52/64 = 81.3% | 53/64 = 82.8% |
| 05-16 `1:13` | 47/60 = 78.3% ✓ | 47/60 = 78.3% |
| 08-05 `21:4` | 116/133 = 87.2% | 113/133 = 85.0% |
| 08-05 `1:13` | 37/49 = 75.5% | 34/50 = 68.0% |

**끼워 맞추지 않는다.** 원 분석의 정확한 규칙은 기획서에 기록돼 있지 않으므로,
근거 있는 정의를 채택하고 실행 결과를 실측값으로 삼는다. Phase 8에서
기획서·Figma 수치를 이 값으로 갱신한다 (계획 §5 폴백 절차).

**서사는 그대로 유지된다** — 약 3개월 간격의 두 독립 주행에서 같은 격자가
81.3% / 87.2%로 재현되었다. 반복성이라는 핵심 주장은 오히려 더 강해졌다.
